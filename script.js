function addFood() {
    const quantity_element = document.getElementById('enter_quantity')
    quantity = Number(quantity_element.value.trim())
    const food_element = document.getElementById('enter_food')
    food = food_element.value.trim()

    if (item === '') {
        
    }
}